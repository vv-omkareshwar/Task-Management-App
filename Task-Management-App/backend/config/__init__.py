// Import the configuration from the config file
import { Config } from './config';

// Export the configuration
export { Config };

// If we need to perform any initialization, we could do it here
// For example:
// Config.init();

// We could also export a default configuration if needed
export default Config;