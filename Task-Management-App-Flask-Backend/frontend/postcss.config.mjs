// postcss.config.js

// PostCSS configuration
// Note: This file uses CommonJS syntax (module.exports) instead of ES6 modules
// to ensure compatibility with PostCSS and the build process.
module.exports = {
  plugins: {
    // Use Tailwind CSS for utility-first CSS
    'tailwindcss': {},
    // Autoprefixer for vendor prefixing
    'autoprefixer': {},
    // Add any other necessary plugins here
  },
}